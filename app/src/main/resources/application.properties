# Application Name
spring.application.name=Foodela

# Server Configuration
server.port=8080

# Database Configuration (PostgreSQL - Production)
spring.datasource.url=jdbc:postgresql://${DB_HOST:db.xfypcimqcipmyodrzzhh.supabase.co}:${DB_PORT:5432}/${DB_NAME:postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Semmozhi12@}
spring.datasource.driver-class-name=org.postgresql.Driver

# H2 Database (Development - Uncomment to use)
# spring.datasource.url=jdbc:h2:mem:foodela
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=YourSuperSecretKeyForJWTAuthenticationThatShouldBeVeryLongAndSecure123456
jwt.expiration=86400000
